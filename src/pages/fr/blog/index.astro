---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import { getAlternateLocalePath } from '../../../lib/locale';
import { buildBlogIndexPath, buildBlogPostPath, formatBlogDate, getPublishedBlogPosts, getReadingTimeMinutes } from '../../../lib/blog';
import { BRAND_LOGO, DEFAULT_OG_IMAGE, SITE_URL, absoluteUrl } from '../../../config/site';

const site = SITE_URL;
const posts = await getPublishedBlogPosts('fr');
const canonical = new URL(buildBlogIndexPath('fr'), site).href;
const alternates = [{ lang: 'en', href: new URL(buildBlogIndexPath('en'), site).href }];
const schema = {
  '@context': 'https://schema.org',
  '@type': 'Blog',
  name: 'Le blog Repero AI',
  description: 'Analyses et retours d’expérience sur l’IA au travail, la mémoire documentaire, les conversations et les artefacts à ne pas perdre.',
  inLanguage: 'fr-FR',
  url: canonical,
  publisher: {
    '@type': 'Organization',
    name: 'Repero AI',
    logo: {
      '@type': 'ImageObject',
      url: absoluteUrl(BRAND_LOGO)
    }
  },
  blogPost: posts.map((post) => ({
    '@type': 'BlogPosting',
    headline: post.data.title,
    description: post.data.description,
    datePublished: post.data.pubDate.toISOString(),
    dateModified: (post.data.updatedDate ?? post.data.pubDate).toISOString(),
    url: new URL(buildBlogPostPath(post), site).href,
    image: absoluteUrl(post.data.image ?? DEFAULT_OG_IMAGE),
    author: {
      '@type': 'Person',
      name: post.data.author
    }
  }))
};
---
<BaseLayout
  lang="fr"
  title="Le blog Repero AI | IA, mémoire de travail et organisation"
  description="Analyses et retours d’expérience sur l’IA au travail, la mémoire documentaire, les conversations et les artefacts à ne pas perdre."
  canonical={canonical}
  alternates={alternates}
  ogType="website"
  twitterCard="summary_large_image"
  schema={schema}
>
  <Header lang="fr" alternateHref={getAlternateLocalePath('fr', alternates)} />
  <main class="page-shell pb-12">
    <section class="section-shell px-6 py-10 md:px-10 md:py-14">
      <div class="max-w-3xl space-y-5">
        <p class="eyebrow">Blog</p>
        <h1 class="headline text-4xl md:text-5xl">Le blog Repero AI</h1>
        <p class="lead max-w-3xl text-lg md:text-xl">
        IA, productivité et gestion des connaissances : guides pratiques pour mieux organiser, retrouver et exploiter vos informations.
        </p>
      </div>
    </section>

    <section class="section-shell mt-8 px-6 py-8 md:px-8 md:py-10">
      <p class="eyebrow">Guides pratiques</p>
      <h2 class="mt-3 text-3xl font-semibold tracking-tight text-white">Trois sujets pour mieux cadrer le travail avec l’IA.</h2>
      <div class="mt-7 grid gap-4 md:grid-cols-3">
        <a class="feature-card hover:border-sky-500/40 hover:bg-slate-900" href="/fr/organiser-conversations-chatgpt"><h3 class="text-xl font-semibold text-white">Organiser les conversations IA</h3><p class="mt-3 text-sm leading-7 text-slate-300">Retrouver les bonnes réponses et en faire du travail durable.</p></a>
        <a class="feature-card hover:border-sky-500/40 hover:bg-slate-900" href="/fr/securiser-ia-pme"><h3 class="text-xl font-semibold text-white">Utiliser l’IA en PME</h3><p class="mt-3 text-sm leading-7 text-slate-300">Travailler avec l’IA sans perdre de vue les données de l’entreprise.</p></a>
        <a class="feature-card hover:border-sky-500/40 hover:bg-slate-900" href="/fr/alternative-europeenne-chatgpt"><h3 class="text-xl font-semibold text-white">GPT, Claude et Mistral dans Repero</h3><p class="mt-3 text-sm leading-7 text-slate-300">Changer d’IA sans disperser les projets et les documents.</p></a>
      </div>
    </section>

    <section class="mt-8 grid gap-5">
      {posts.map((post) => {
        const href = buildBlogPostPath(post);
        const readingTime = getReadingTimeMinutes(post.body);

        return (
          <article class="feature-card">
            <div class="flex flex-wrap items-center gap-3 text-sm text-slate-400">
              <span class="chip">{post.data.category}</span>
              <span>{formatBlogDate(post.data.pubDate)}</span>
              <span>{readingTime} min de lecture</span>
            </div>
            <h2 class="mt-4 text-2xl font-semibold text-white">
              <a class="hover:text-sky-300" href={href}>{post.data.title}</a>
            </h2>
            <p class="mt-3 max-w-3xl text-base leading-7 text-slate-300">{post.data.description}</p>
            <div class="mt-5">
              <a class="btn-secondary" href={href}>Lire l’article</a>
            </div>
          </article>
        );
      })}
    </section>
  </main>
  <Footer lang="fr" />
</BaseLayout>
